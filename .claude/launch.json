{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "houmon-dev",
      "runtimeExecutable": "/bin/sh",
      "runtimeArgs": ["-c", "cd /tmp/houmon-app && rm -rf .next && PATH=$HOME/.local/bin:$PATH npx next dev"],
      "port": 3000,
      "autoPort": true
    }
  ]
}
