-- ──────────────────────────────────────────────────────────────
-- 2026-04-25 「行きたい」ブックマーク機能のためのカラム追加
--
-- 使い方: Supabase ダッシュボード > SQL Editor で
--         このファイル全体を貼り付けて Run。
--         IF NOT EXISTS 付きなので何度実行しても安全。
-- ──────────────────────────────────────────────────────────────

ALTER TABLE members
  ADD COLUMN IF NOT EXISTS want_to_visit BOOLEAN NOT NULL DEFAULT FALSE;

-- 既存行は DEFAULT で全員 false が入る。アプリ側のフォールバックにより、
-- 万が一カラム反映前にアプリが動いても落ちないように作ってある。
